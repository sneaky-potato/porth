mem 1  + 1 .
mem 28 + 1 .

// number of generations
0 while dup 30 < do
    0 while dup 30 < do
        // if (mem[j] == 1)
        dup mem + , if
            mem 30 + 42 .
        else
            mem 30 + 32 .
        end
        1 mem 30 + 1 1 syscall3
        1 +
    end
    // pop the inner loop counter
    drop
    mem 30 + 10 .
    1 mem 30 + 1 1 syscall3
    1 +
end
